const express=require("express"),c=require("../controllers/paymentController"),auth=require("../middleware/authMiddleware");const r=express.Router();r.post("/webhook",c.webhook);r.use(auth);r.get("/packages",c.getPackages);r.post("/packages",c.createPackage);r.put("/packages/:id",c.updatePackage);r.delete("/packages/:id",c.deletePackage);r.get("/payments",c.getPayments);r.post("/payments",c.createPayment);r.post("/payments/verify",c.verifyPayment);r.patch("/payments/:id/paid",c.markPaymentPaid);r.delete("/payments/:id",c.deletePayment);r.get("/payments/:id/invoice",c.downloadInvoice);r.get("/client-packages",c.getClientPackages);module.exports=r;
